# WorkNote 로그인 로딩 UI 개선 - 현재 버전

> 이 문서는 기존 Modern Loading 가이드의 최신화 버전입니다. 현재 로그인 로딩 기준은 약 2분 30초 예상 진행률 + 최대 10분 Timeout입니다.

# WorkNote 로그인 예상 진행률 / 10분 Timeout 적용 가이드

## 1. 적용 목적

Render 백엔드가 대기 상태에서 다시 시작될 때 실제 프로젝트에서 약 2분 30초 정도 기다리는 상황을 기준으로,
로그인 화면에서 사용자가 현재 요청이 진행 중임을 확인할 수 있도록 예상 진행률, 예상 시간, 경과 시간을 표시한다.

중요: 이 진행률은 Render가 실제 부팅률을 제공해서 표시하는 값이 아니다.
클라이언트가 경과 시간을 기준으로 계산하는 **예상 진행률**이다.

## 2. 진행률 정책

기본값:

- 예상 Cold Start: 150,000ms = 2분 30초
- 로그인 최대 Timeout: 600,000ms = 10분
- 250ms보다 빠른 로그인은 큰 오버레이를 생략할 수 있음
- 오버레이가 표시되면 최소 400ms 유지
- 실제 로그인 성공 응답이 오면 즉시 100%
- 성공 상태를 약 500ms 보여준 뒤 `/dashboard`로 이동

예상 진행률 계산:

- 0 ~ 2분 30초: 약 1% → 95%
- 2분 30초 초과: 95% → 최대 99%까지 아주 천천히 증가
- 실제 로그인 성공 응답: 즉시 100%
- 10분 동안 응답 없음: Axios timeout으로 실패 처리

2분 30초가 지났는데 실제 응답이 없을 때 100%를 표시하지 않는 이유는,
100%가 사용자에게 '서버가 준비되었다'는 의미로 보일 수 있기 때문이다.
따라서 실제 성공 응답이 오기 전에는 최대 99%로 제한한다.

## 3. 한국어 문구

예상 시간 내:
- 상태: 서버 시작을 기다리는 중
- 제목: 서버를 준비하고 있습니다
- 설명: 서버가 대기 상태라면 다시 준비되는 데 평균 약 2분 30초를 예상하고 있습니다. 실제 서버 응답이 더 빠르게 도착하면 진행률을 즉시 100%로 완료하고 로그인합니다.
- 안내: 표시되는 진행률은 서버가 보내는 실제 부팅률이 아니라 예상 대기 시간을 기준으로 한 안내입니다.

2분 30초 초과:
- 상태: 예상 시간을 넘어 연결 대기 중
- 제목: 예상 시간보다 조금 더 걸리고 있습니다
- 설명: 서버 응답을 계속 기다리며 최대 10분까지 기다린다.
- 진행률: 최대 99%

성공:
- 상태: 서버 응답 확인 완료
- 제목: 서버 연결이 완료되었습니다
- 진행률: 100%
- 약 500ms 후 Dashboard 이동

10분 Timeout:
- `10분 동안 서버의 응답이 없어 로그인을 중단했습니다. 현재 서버에 문제가 있거나 일시적으로 사용할 수 없는 상태일 수 있습니다. 잠시 후 다시 시도해 주세요.`

## 4. 일본어 문구

예상 시간 내:
- 状態: サーバーの起動を待っています
- タイトル: サーバーを準備しています
- 説明: サーバーが休止状態の場合、再び利用可能になるまでの目安は約2分30秒。実際の応答が早ければ進行率をすぐ100%にしてログインを続行。
- 注意: 表示している進行率は実際のサーバー起動率ではなく、推定待ち時間を基準にした目安。

2分30秒超過:
- 状態: 目安時間を超えて応答待機中
- タイトル: 目安時間より少し長くかかっています
- 最大10分まで待機
- 実応答前は最大99%

成功:
- サーバー応答確認後100%
- 約500ms後Dashboardへ移動

10分Timeout:
- `10分間サーバーから応答がなかったため、ログインを中断しました。現在サーバーに問題があるか、一時的に利用できない可能性があります。しばらくしてからもう一度お試しください。`

## 5. 환경변수

프론트의 `.env.example`에 다음 값을 추가했다.

```env
VITE_API_BASE_URL=https://worknote-backend-8imu.onrender.com
VITE_LOGIN_COLD_START_ESTIMATE_MS=150000
VITE_LOGIN_TIMEOUT_MS=600000
```

로컬:
- `.env` 또는 `.env.local`에 설정 가능
- 설정하지 않아도 코드의 기본값 150000 / 600000 사용

Vercel:
- Project Settings -> Environment Variables에 같은 이름으로 등록 가능
- `VITE_` 변수는 브라우저 번들에 포함되는 공개 설정값이므로 Secret을 넣으면 안 됨
- 값을 바꾸면 새 배포가 필요함

Render / Supabase:
- 이번 로그인 진행률 기능 때문에 추가할 환경변수 없음
- 기존 Backend DB/JWT/Ollama/OCR 환경변수 그대로 유지

## 6. Timeout 범위

기존 `src/api/api.js`의 기본 Axios timeout 180,000ms는 유지한다.

로그인 요청에서만:

```js
api.post("/api/auth/login", body, {
  timeout: LOGIN_TIMEOUT_MS,
})
```

로 10분 timeout을 덮어쓴다.

이렇게 한 이유:
- OCR/일반 API 요청까지 모두 10분으로 늘리지 않기 위해서
- Cold Start 영향을 직접 받는 로그인 요청만 별도로 긴 대기시간을 허용하기 위해서

## 7. 인프라 응답 관련 주의

클라이언트는 최대 10분을 기다리도록 설정했지만,
Render/Vercel/브라우저 네트워크 계층에서 그 전에 5xx 또는 네트워크 오류를 반환하면 로그인은 더 일찍 실패할 수 있다.
이 경우에는 10분을 억지로 채우지 않고 실제 오류 응답을 사용자에게 안내하는 것이 맞다.

## 8. 변경 파일

- `src/pages/LoginPage.jsx`
- `src/pages/AuthPage.css`
- `src/i18n/translations.js`
- `.env.example`

`LanguageContext.jsx`의 브라우저 KO/JA 자동 감지 로직은 그대로 유지한다.
Backend, Render, Supabase 코드는 이번 변경으로 수정하지 않는다.
